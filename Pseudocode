Host Blacklisting

MUST indlude: File I/O, imported modules, functions, error detection, and command line arguments. 
             File I/O: (etc/hosts on Linux, c:\Windows\System32\Drivers\etc\hosts on windows) 
             Imported Modules: OS, Sys, Argparse....

#Create a command line interface where you can enter a host FQDN to be blocked similar to DNS blackholing. 
import argparse, sys, os,

#module will need to work indendent of the operating system
import platform

getHostPath():
    #determine the path to the HOSTS file, irrespective of the OS
    if system == "Windows":
       return "C:\Windows\System32\drivers\etc\hosts"
    elif system == "Linux":
       return "/etc/hosts"
    else:
       #for MacOS and other OSes
       return "/etc/hosts"

#The script will use the hosts file to direct any requests to the specified host to 0.0.0.0. 
File I/O

#If the specified entry already exists in the host file, the script must inform the user the host already exists and ask if the entry should be deleted or modified.
Error handling/ File I/O

