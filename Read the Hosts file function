Read the Hosts file function

pathToHost = getHostPath()

#the address used to block the suspect FQDN
dns_blackhole = "0.0.0.0"

def hosts_file():
    with open(pathToHost, 'r') as hostFile:
        return hostFile.readlines()
